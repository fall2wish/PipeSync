name: pipesync
description: High-performance, secure cross-platform data pipeline engine with 2PC Verify & Purge.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  ffi: ^2.1.0
  sqlite3: ^2.1.0
  path: ^1.8.3
  crypto: ^3.0.3

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
